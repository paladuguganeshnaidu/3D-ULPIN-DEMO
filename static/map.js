(() => {const map=L.map('main-map').setView([window.CENTER.lat,window.CENTER.lng],16);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);const bounds=[];window.BUILDINGS.forEach(b=>{const risk=b.floors>b.sanctioned_floors;const footprint=b.footprint||[[b.lat,b.lng]];const layer=footprint.length>2?L.polygon(footprint,{color:risk?'#ffcf70':'#55e6a5',weight:2,fillColor:risk?'#ff9f43':'#55e6a5',fillOpacity:.32}).addTo(map):L.circleMarker([b.lat,b.lng],{radius:9,color:'#071015',weight:2,fillColor:'#55e6a5',fillOpacity:.95}).addTo(map);footprint.forEach(point=>bounds.push(point));const flats=(b.floors||0)*2;layer.bindPopup(`<div class="popup"><h3>${b.building_name}</h3><p><b>ULPIN:</b> ${b.ulpin}</p><p>${b.address}</p><p>${b.floors} floors · ${flats} mapped flats</p><p>${b.height} m height · ${b.usage}</p>${risk?'<p style="color:#ffd27a">⚠ Potential vertical violation</p>':''}<div class="pb"><a href="/view/${b.id}">Open 3D view</a> <a href="/api/buildings/${b.id}" target="_blank">JSON record</a></div></div>`);});if(bounds.length) map.fitBounds(bounds,{padding:[40,40]});})();
