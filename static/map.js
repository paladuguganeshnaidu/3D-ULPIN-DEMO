(() => {const map=L.map('main-map').setView([window.CENTER.lat,window.CENTER.lng],16);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);const bounds=[];window.BUILDINGS.forEach(b=>{const marker=L.circleMarker([b.lat,b.lng],{radius:9,color:'#071015',weight:2,fillColor:'#55e6a5',fillOpacity:.95}).addTo(map);bounds.push([b.lat,b.lng]);const risk=b.floors>b.sanctioned_floors;marker.bindPopup(`<div class="popup"><h3>${b.building_name}</h3><p><b>ULPIN:</b> ${b.ulpin}</p><p>${b.address}</p><p>${b.floors} observed floors · ${b.sanctioned_floors} sanctioned</p><p>${b.height} m height · ${b.usage}</p>${risk?'<p style="color:#ffd27a">⚠ Potential vertical violation</p>':''}<div class="pb"><a href="/view/${b.id}">Open 3D view</a></div></div>`);});if(bounds.length) map.fitBounds(bounds,{padding:[40,40]});})();
